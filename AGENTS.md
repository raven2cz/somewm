# Agent Bootstrap

Before making changes in this repository, read `CLAUDE.md`. It contains the
project workflow, deploy/reload commands, diagnostics, and fork-specific
operational rules used by this somewm checkout.

If the task touches deploy/reload, memory diagnostics, upstream sync, or
NVIDIA-specific behavior, use the relevant section of `CLAUDE.md` as the
source of truth. The user config (`somewm-one`) and Quickshell desktop shell
(`somewm-shell`) now live in their own repos — see CLAUDE.md "Sibling repos"
for paths and override env vars.
