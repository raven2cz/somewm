# Agent Bootstrap

Before making changes in this repository, read `CLAUDE.md`. It contains the
project workflow, deploy/reload commands, diagnostics, and fork-specific
operational rules used by this somewm checkout.

If the task touches `plans/project/somewm-one`, `plans/project/somewm-shell`,
deploy/reload, memory diagnostics, upstream sync, or NVIDIA-specific behavior,
use the relevant section of `CLAUDE.md` as the source of truth.
